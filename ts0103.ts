// Tuples

// tuple definition - 
let address: [string, number] = ["my streeet", 3];

// enum 
enum Color {
    Gray, // 0
    Green, // 1
    Blue // 2
};

let myColor: Color = Color.Blue;
console.log(myColor);

// any type
let car: any = "BMW";
console.log(car);
